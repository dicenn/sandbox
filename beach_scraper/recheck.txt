# Start dates for MODE=dates (one ISO date per line).
# Push with [recheck] in the commit message to run these.
#
# Set NIGHTS=6,7,8 to fill in +/-1 night on dates worth a closer look;
# it defaults to 7.
#
# Currently: two of the dates the baseline could not price. The page reports
# "60 ROOMS FOUND" for these, so they are NOT sold out — the price parser
# fails on them. This run captures the markup that defeats it.
2027-03-24
2027-03-28
